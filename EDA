# Frequently used code for EDA

# EDA of Numerical Values Continued: Visualize Each Numerical Variable Using A Boxplot
# 3 plots per row

column_list = list(df.columns)

for i in range(0, len(column_list), 3):
    plt.figure(figsize=(12,3))
    plt.subplot(131)
    sns.boxplot(x=df[column_list[i]])
    plt.subplot(132)
    sns.boxplot(x=df[column_list[i+1]])
    plt.subplot(133)
    sns.boxplot(x=df[column_list[i+2]])
    #plt.tight_layout()
    plt.show()
